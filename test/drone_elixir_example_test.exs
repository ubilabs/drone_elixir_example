defmodule DroneElixirExampleTest do
  use ExUnit.Case

  test "the truth" do
    assert(true)
  end

  test "simple math" do
    assert 1 + 2 == 3
  end

  test "simple math 2" do
    assert 10 + 20 == 30
  end
end
